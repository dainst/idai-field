{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "@fontsource/roboto": "5.1.1"
  },
  "scripts": {
    "bootstrap": "npm i && lerna bootstrap -- --force",
    "build": "lerna run build"
  }
}
