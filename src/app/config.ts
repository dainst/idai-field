export const CONFIG = {
	//environment: 'production', // choose 'test', 'development' or 'production'
	environment: 'test', // choose 'test', 'development' or 'production'
    //backendUri: 'http://localhost:4567', // should be specified without ending /
    backendUri: 'http://localhost:9200/idaifield', // should be specified without ending /
    credentials: 'admin:s3cr3t',
    backendConnectionCheckInterval: 1000
};